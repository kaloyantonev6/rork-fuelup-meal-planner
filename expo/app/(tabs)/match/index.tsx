import React from "react";

import MatchDayScreen from "../../match-day";

/** Match Day tab — renders the same Match Day screen exposed at /match-day. */
export default function MatchTab() {
  return <MatchDayScreen />;
}
